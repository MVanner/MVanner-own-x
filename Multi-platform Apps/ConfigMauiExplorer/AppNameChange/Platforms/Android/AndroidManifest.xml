<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:roundIcon="@mipmap/appicon_round" android:icon="@mipmap/appicon" android:allowBackup="true" android:supportsRtl="true">

        <activity-alias
                android:name=".MainActivityExtra"
                android:targetActivity=".MainActivity"
                android:enabled="false"
                android:exported="true"
                android:label="@string/app_name"
                android:icon="@mipmap/appiconnew"
                android:roundIcon="@mipmap/appiconnew_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>